import useTitle from "../../../hooks/useTitle";


const Classes = () => {
    useTitle('classes')
    
    return (
        <div>
            <p>This is classes page</p>
        </div>
    );
};

export default Classes;